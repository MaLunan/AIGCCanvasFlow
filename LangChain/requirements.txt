# Web 框架
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# LangChain
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0

# Pydantic settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 任务队列
celery[redis]>=5.3.0
redis>=5.0.0

# HTTP 客户端
httpx>=0.27.0
openai>=1.30.0

# 图片处理
Pillow>=10.0.0

# 对象存储
minio>=7.2.0
boto3>=1.34.0        # 备用：AWS S3 / 阿里云 OSS S3 兼容接口

# 模型平台 SDK
replicate>=0.25.0    # Flux / SDXL via Replicate
PyJWT>=2.8.0         # 可灵 JWT 签名

# 环境变量
python-dotenv>=1.0.0

# 进程编排（make all / Procfile）
honcho>=1.1.0
